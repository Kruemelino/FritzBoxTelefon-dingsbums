<UserControl x:Class="WatermarkTextBox"
             x:Name="WatermarkTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"         
             mc:Ignorable="d">

    <Grid DataContext="{Binding ElementName=WatermarkTextBox}">

        <TextBox x:Name="SearchTermTextBox" Text="{Binding Path=Text, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock IsHitTestVisible="False" Text="{Binding Path=Watermark}" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="2,0,0,0" Foreground="DarkGray">
            <TextBlock.Style>
                <Style TargetType="{x:Type TextBlock}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Text, ElementName=SearchTermTextBox}" Value="">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
    </Grid>
</UserControl>
